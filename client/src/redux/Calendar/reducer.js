
import { CONSTANTS } from "./constants";


const initialState = {
    date: {}
  };
  
  const calendary = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state
    }

  }

  export default calendary;
